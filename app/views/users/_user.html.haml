%li.user
  .user-icon
    = link_to image_tag(user.profile_image_url), user_path(user)
  .user-info
    .login= link_to user.login, user_path(user)
    .description= user.description
    .points
      %strong= user.yums.enables.sum(:view_count)
      views
      %strong= user.receive_comments_count
      comments
      %strong= user.yums.enables.count
      photos
      %strong= user.yums.enables.sum(:yummy_count)
      pecos
  - if rank
    .rank= rank
